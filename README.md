# 무드 페어링 (Mood Pairing)

> 제목도 가수도 모르는 곡을, 문장으로 찾습니다.

지금의 기분·상황·취향을 자유롭게 문장으로 적으면, AI가 그 맥락을 해석해
**실존이 검증된 곡 3~5개**와 **그 순간을 위해 새로 쓰인 짧은 글**을 함께 건네는 웹 서비스입니다.

**배포 URL:** https://mood-pairing.vercel.app *(배포 후 실제 주소로 교체)*

---

## 왜 만들었나

모든 음원 앱의 검색창은 곡명·가수명이라는 **고유명사**를 전제합니다.
그런데 우리가 음악을 찾는 동기는 대개 이름이 아니라 상태입니다.

- "가사는 밝은데 멜로디는 쓸쓸한 곡"
- "후렴에서 터뜨리지 않고 끝까지 눌러 담는 곡"
- "연주자의 해석을 빼고 악보 그대로 친 연주"

말로는 분명히 설명되는데 검색어로는 옮길 수 없는 조건들입니다.
그 문장을 그대로 받는 입구를 만드는 것이 이 서비스의 목적입니다.

---

## 핵심 설계: 환각 없는 추천

LLM에게 곡을 추천하게 하면 **존재하지 않는 곡을 실존 가수 이름으로 지어내는** 문제가 생깁니다.
이 서비스는 AI에게 "무엇을 찾을지"만 맡기고, **실존 여부는 실제 음원 데이터베이스가 판정**합니다.

```
사용자 자연어 입력
      │
      ▼
[1] Claude API  →  무드 해석 + 후보곡 N곡 + 검색 키워드 + 창작 문장 (JSON 고정 스키마)
      │
      ▼
[2] iTunes Search API로 후보곡을 하나씩 대조 (병렬 조회)
      │
      ▼
[3] 검증 통과한 곡만 → 앨범아트 + 30초 미리듣기 + 음원 링크
      (통과가 2곡 미만이면 검색 키워드로 보충)
```

문학 인용도 같은 이유로 쓰지 않습니다. 저작권 문제가 있고, LLM이 인용문을 지어낼 위험이 크기 때문에
**기존 작품을 인용하는 대신 매번 새로 쓰게** 했습니다.

---

## 기술 스택

| 영역 | 사용 기술 |
|---|---|
| 프론트엔드 | HTML · CSS · Vanilla JavaScript (프레임워크 없음) |
| 백엔드 | Vercel Serverless Functions (Python 3.12) |
| AI | Claude API (`claude-opus-5`) — 구조화 출력(JSON Schema) |
| 음원 데이터 | iTunes Search API (API 키 불필요) |
| 저장소 | 브라우저 localStorage (검색 기록 · 서버 전송 없음) |
| 배포 | GitHub → Vercel 자동 배포 |

---

## 폴더 구조

```
mood-pairing/
├── index.html          # 단일 페이지 (5개 섹션 + 앵커 네비게이션)
├── css/
│   └── style.css       # 반응형 스타일 (모바일 375 / 태블릿 768 / 데스크톱 1280)
├── js/
│   └── main.js         # 폼 검증, fetch, 결과 렌더, 미리듣기, 검색 기록
├── api/
│   └── pair.py         # 서버리스 함수 — Claude 호출 + iTunes 실존 검증
├── requirements.txt    # Python 의존성
├── vercel.json         # 함수 실행 시간 설정
├── .env.example        # 환경 변수 템플릿
└── .gitignore
```

---

## 환경 변수 설정

### 필요한 키

| 이름 | 설명 | 필수 |
|---|---|---|
| `ANTHROPIC_API_KEY` | Claude API 키 ([발급](https://console.anthropic.com/settings/keys)) | ✅ |

iTunes Search API는 **키가 필요 없습니다.**

### 로컬 개발

```bash
cp .env.example .env
```

`.env` 파일을 열어 실제 키를 채워 넣습니다. 이 파일은 `.gitignore`에 등록되어 있어 커밋되지 않습니다.

### Vercel 배포 환경

Vercel 대시보드 → 프로젝트 → **Settings → Environment Variables** 에서 추가합니다.

- Name: `ANTHROPIC_API_KEY`
- Value: 발급받은 키
- Environment: Production, Preview, Development 모두 체크

> ⚠️ **키를 코드·README·스크린샷에 절대 노출하지 마세요.**
> 실수로 커밋했다면 즉시 콘솔에서 해당 키를 폐기(revoke)하고 새로 발급받은 뒤,
> 노출된 커밋 이력도 정리해야 합니다. 키를 지우는 커밋을 추가하는 것만으로는
> 이전 커밋에 남아 있는 키가 사라지지 않습니다.

---

## 실행 방법

### 로컬 실행

Vercel Serverless Functions를 로컬에서 돌리려면 Vercel CLI가 필요합니다.

```bash
npm i -g vercel
```

```bash
vercel dev
```

`http://localhost:3000` 에서 프론트와 `/api/pair` 가 함께 동작합니다.

> `python -m http.server` 같은 정적 서버로 열면 화면은 뜨지만 `/api/pair` 호출이 404가 납니다.
> AI 기능을 확인하려면 `vercel dev`를 쓰세요.

### 배포

```bash
git push origin main
```

GitHub 저장소를 Vercel에 연결해두면 push할 때마다 자동 배포됩니다.
최초 1회는 Vercel 대시보드에서 **Add New → Project → GitHub 저장소 선택** 으로 연결하고,
위의 환경 변수를 등록한 뒤 Deploy를 누르면 됩니다.

---

## 페이지 구성

| 섹션 | 내용 |
|---|---|
| 홈 (Hero) | 서비스 소개 + 바로 써보기 |
| 무드 페어링 | 핵심 기능 — 입력 폼 → 결과 카드(무드 요약 / 곡 / 창작 문장) |
| 이렇게 검색해보세요 | 좋은 입력 vs 아쉬운 입력 비교, 3단계 가이드 |
| 검색 기록 | 열람 · 재검색 · 개별 삭제 · 전체 삭제 · 내보내기 |
| FAQ · 만든 이유 | 동작 원리, AI 추천의 한계 고지, 사용 기술 |

상단 고정 네비게이션으로 이동하며, 모바일에서는 햄버거 메뉴로 전환됩니다.

---

## AI 기능 명세

**입력**
- 자유 텍스트 (필수, 10~500자)
- 선택 옵션: 가사 유무(상관없음/있음/없음), 곡 개수(3/5)
- 조정 (선택): 칩 클릭 또는 **자유 입력** — "드럼이 더 또렷했으면" 같은 문장도 그대로 반영

**출력**

| 필드 | 내용 |
|---|---|
| `mood_summary` | 입력을 어떻게 읽었는지 한 문장 |
| `tracks[]` | 검증된 곡 — 제목, 가수, 앨범아트, 30초 미리듣기, 음원 링크, 추천 이유 |
| `original_text` | 이 순간을 위해 새로 쓰인 2~3문장 |
| `keywords[]` | 음원 앱에 그대로 넣어볼 수 있는 검색어 3개 |

**실패 처리**

| 상황 | 사용자에게 보이는 것 |
|---|---|
| 빈 입력 / 10자 미만 | "조금만 더 자세히 적어주세요. 길수록 정확해져요." |
| 500자 초과 | 실시간 글자수 카운터 경고 + 제출 차단 |
| API 오류 (4xx / 5xx) | "지금은 곡을 찾지 못했어요. 잠시 후 다시 시도해주세요." |
| 15초 초과 | `AbortController`로 중단 → "응답이 지연되고 있어요." |
| 검증 통과 곡 0개 | 키워드 검색으로 보충 → 그래도 없으면 다시 표현해달라고 안내 |
| 위기 표현 감지 | 추천 대신 상담 안내(자살예방 상담전화 109) 노출 |

---

## 개인정보 및 데이터 처리

- **검색 기록은 브라우저 localStorage에만 저장**되며 서버로 전송되지 않습니다.
- 기록은 **열람 전용**입니다. 다음 검색 추천에 반영되지 않습니다.
  추천 알고리즘이 사용자를 자기 이력에 가두는 문제를 피하기 위한 의도적인 설계입니다.
- 서버는 요청받은 문장을 Claude API로 전달할 뿐 별도로 저장하지 않습니다.
- 전체 삭제 버튼으로 즉시 지울 수 있고, 텍스트 파일로 내보낼 수 있습니다.

---

## 알려진 제약

- iTunes Search API에 요청 빈도 제한이 있습니다(대략 분당 20회 수준으로 알려짐).
  후보곡 검증에 곡당 1회씩 요청하므로, 연속으로 여러 번 검색하면 일시적으로 결과가 줄 수 있습니다.
- `previewUrl`은 선택 필드라 미리듣기가 없는 곡이 있습니다. 이 경우 재생 버튼이 비활성화됩니다.
- 동명이곡이나 리메이크 버전이 매칭될 수 있습니다. 미리듣기로 확인을 권합니다.

---

## 라이선스 / 출처

음원 메타데이터, 앨범 아트, 30초 미리듣기는 Apple iTunes Search API를 통해 제공됩니다.
